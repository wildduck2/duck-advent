// Bumped by scripts/release. The compiled binary inlines this constant so it
// doesn't have to read package.json at runtime (bun --compile sandboxes the fs).
export const DUCK_ADVENT_VERSION = "0.1.0";
